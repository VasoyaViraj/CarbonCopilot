# --- Server ---
AI_SERVICE_PORT=8000
LOG_LEVEL=INFO

# --- Backend (deterministic business APIs; source of factory data) ---
BACKEND_INTERNAL_URL=http://localhost:5000/api
# Must match backend AI_SERVICE_TOKEN.
AI_SERVICE_TOKEN=""

# --- LLM (reasoning / explanation / orchestration only; never authoritative arithmetic) ---
LLM_PROVIDER=""
LLM_MODEL=""
LLM_API_KEY=""
LLM_TEMPERATURE=0
LLM_TIMEOUT_SECONDS=30
